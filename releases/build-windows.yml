name: Build Windows App

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build Installer
    runs-on: windows-latest

    steps:
      # 1. 拉取代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 配置 Flutter 环境 (锁定 3.27.0)
      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'
          channel: 'stable'
          cache: true

      # 3. 安装依赖
      - name: Install Dependencies
        run: flutter pub get

      # 4. 开始编译
      - name: Build Windows
        run: flutter build windows --release

      # === 🔍 调试步骤 (修正版) ===
      - name: Check Exe Name
        shell: powershell
        run: |
          # 改用英文，避免编码报错
          Write-Host "Checking Release directory for .exe files:"
          
          # ⚠️ 注意：这里加上了 x64，因为 Flutter 3.10+ 默认输出到 x64 目录
          Get-ChildItem build\windows\x64\runner\Release\*.exe | Select-Object Name

      # 5. 生成安装包 (Inno Setup)
      - name: Build Installer
        shell: powershell
        run: |
          choco install innosetup --no-progress
          
          # [修正] 使用正确的 Unofficial 目录链接下载中文语言包
          $LangUrl = "https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/Unofficial/ChineseSimplified.isl"
          
          echo "正在下载语言包..."
          Invoke-WebRequest -Uri $LangUrl -OutFile "installers\ChineseSimplified.isl"
          
          # 检查文件是否真的下载成功 (调试用)
          if (Test-Path "installers\ChineseSimplified.isl") {
              echo "✅ 语言包下载成功"
          } else {
              echo "❌ 语言包下载失败"
              exit 1
          }
          
          echo "开始编译..."
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "installers\setup.iss"

      # 6. 上传产物
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-app
          path: build/windows/installer/*.exe
          retention-days: 3